# Photobooth deployment configuration (systemd EnvironmentFile)
# Copy to /etc/photobooth.env and edit for each event.

# Where sessions/ and generated images will be stored.
PHOTOBOOTH_IMAGE_ROOT=/var/lib/photobooth

# Event album code (operator-provided)
PHOTOBOOTH_ALBUM_CODE=CHANGE_ME

# Path to the event logo used in strip/print rendering
PHOTOBOOTH_LOGO_PATH=/var/lib/photobooth/logo.png
